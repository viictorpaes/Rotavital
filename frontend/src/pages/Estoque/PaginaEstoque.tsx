import { EmBreve } from "@/components/ui/EmBreve";

export default function PaginaEstoque()
{
  return (
    <EmBreve
      titulo="Estoque"
      descricao="Estoque agrupado por tipo sanguíneo, com validade e temperatura por lote (HU-03) — em desenvolvimento."
    />
  );
}
