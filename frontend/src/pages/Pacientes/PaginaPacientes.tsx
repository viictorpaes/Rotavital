import { EmBreve } from "@/components/ui/EmBreve";

export default function PaginaPacientes()
{
  return (
    <EmBreve
      titulo="Pacientes"
      descricao="Conclusão de procedimento com baixa por FEFO ou doação externa (HU-08) — em desenvolvimento."
    />
  );
}
