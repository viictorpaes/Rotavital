import { EmBreve } from "@/components/ui/EmBreve";

export default function PaginaPainel()
{
  return (
    <EmBreve
      titulo="Painel Operacional"
      descricao="Números rápidos e caixa de avisos com alertas críticos (HU-02) — em desenvolvimento."
    />
  );
}
