import { EmBreve } from "@/components/ui/EmBreve";

export default function PaginaRede()
{
  return (
    <EmBreve
      titulo="Rede Hospitalar"
      descricao="Mapa de hospitais conectados e plano de transporte na cadeia fria (HU-07) — em desenvolvimento."
    />
  );
}
